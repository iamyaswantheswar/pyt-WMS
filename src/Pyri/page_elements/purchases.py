import tkinter as ui
class purchases_elements:
	def purchases_ele(self,frame):
                self.lbl = ui.Label(frame, text="welcome to purchases")
                self.lbl.place(relx=0.5, rely=0.5, anchor="center")
