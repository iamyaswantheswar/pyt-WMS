import tkinter as ui
class sales_elements:
	def sales_ele(self,frame):
                self.lbl = ui.Label(frame, text="welcome to sales")
                self.lbl.place(relx=0.5, rely=0.5, anchor="center")
