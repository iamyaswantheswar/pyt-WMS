import tkinter as ui
class inventory_elements:
	def inventory_ele(self,frame):
                self.lbl = ui.Label(frame, text="welcome to inventory")
                self.lbl.place(relx=0.5, rely=0.5, anchor="center")
