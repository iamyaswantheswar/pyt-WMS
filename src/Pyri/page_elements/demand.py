import tkinter as ui
class demand_elements:
	def demand_ele(self,frame):
                self.lbl = ui.Label(frame, text="welcome to Demand")
                self.lbl.place(relx=0.5, rely=0.5, anchor="center")
